{
  "name": "movie-final-project",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "concurrently": "^8.2.0",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.3",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "npm run development",
    "development": "concurrently \"npm run client\" \"npm run server\"",
    "production": "npm run build && npm run server",
    "server": "node server/server.js",
    "client": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  },
  "engines": {
    "node": "20.x"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
